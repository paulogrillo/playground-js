/* Quando usamos (var) a saída abaixo retorna o último valor adicionado (React)
 * Utilizar (var) implica em várias desvantagens a principal é duplicidade de variáveis. */

var framework = "Angular";
var framework = "React";

/* console.log(framework) */


/* Quando usamos (let) a saída abaixo retorna um erro.
Informando que a variável já foi declarada anteriormente. */
let language = "Java";
let language = "Python";

console.log(language);
